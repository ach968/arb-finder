import { Box, Button } from "@mui/material";

const CardFooter: React.FC = () => {
    return (
        <Box textAlign="center" padding={2}>
            <Button variant="contained" color="primary">Take Bet</Button>
        </Box>
    );
};

export default CardFooter;
